import { DataBase } from "./Database/Database";

(async () => {
    // const myDataBase = new DataBase('C:/DataBase', 1, '/data.txt');
    // await myDataBase.createDatabase();
    // const dada = await myDataBase.getData();
    // console.log(dada);

})();