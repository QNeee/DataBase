// import express from 'express';
// import { asyncWrapper } from '../helpers/apiHelper'
// import { postDataController } from '../controllers/dataController';

// export const dataRoutes = express.Router();

// dataRoutes.post('/', asyncWrapper(postDataController));